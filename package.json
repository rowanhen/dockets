{
  "name": "@leitware/dockets",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '@leitware/dockets' build",
    "dev": "bun run --cwd packages/showcase dev",
    "test": "bun run --filter '@leitware/dockets' test",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
